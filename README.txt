To be able to run the client project you need to have NodeJs (nodejs.org) installed on your computer. 

To run the test project use the developer command prompt for visual studio. Navigate to the test project folder and run "dotnet test". 